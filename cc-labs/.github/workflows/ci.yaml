name: CoderCO CICD workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: we are testing this workflow
        run: echo Hello, my people!
      